// Generated by scaffold-language-toggle — DO NOT EDIT BY HAND. Re-run to refresh.
import { useTranslation } from 'react-i18next';
import { Button } from '~/components/ui/button';
import { DropdownMenu, DropdownMenuContent, DropdownMenuItem, DropdownMenuTrigger } from '~/components/ui/dropdown-menu';

const LOCALES = [{"code":"en","label":"English"},{"code":"ko","label":"한국어"}] as const;

export function LanguageToggle() {
  const { i18n } = useTranslation();
  const current = LOCALES.find((l) => l.code === i18n.language) ?? LOCALES[0];
  return (
    <DropdownMenu>
      <DropdownMenuTrigger asChild>
        <Button variant="ghost" size="sm" data-testid="language-toggle">
          {current.label}
        </Button>
      </DropdownMenuTrigger>
      <DropdownMenuContent align="end" data-testid="language-toggle-menu">
        {LOCALES.map((l) => (
          <DropdownMenuItem
            key={l.code}
            onClick={() => { void i18n.changeLanguage(l.code); }}
            data-testid={`language-toggle-${l.code}`}
          >
            {l.label}
          </DropdownMenuItem>
        ))}
      </DropdownMenuContent>
    </DropdownMenu>
  );
}
