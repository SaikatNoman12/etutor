/**
 * useAppSelector.ts — shorthand re-export of the typed selector hook.
 *
 * The canonical implementation lives in ~/redux/store/hooks.ts. Matches the
 * Redux Toolkit recommended import pattern; pairs with useAppDispatch.ts.
 */
export { useAppSelector } from '~/redux/store/hooks';
