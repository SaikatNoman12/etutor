/** Generated by scaffold-dashboard-pages. */
import { layout, route } from '@react-router/dev/routes';

// The dashboard, behind the same guard and inside the same chrome as every
// other console screen (see safety-net.routes.ts).
export const adminOverviewRoutes = [
  layout("components/guards/AdminGuard.tsx", { id: "adminoverview-guard" }, [
    layout("components/layouts/AdminLayout.tsx", { id: "adminoverview-admin-layout" }, [
      route('admin', 'pages/admin/adm-01-dashboard.tsx'),
    ]),
  ]),
];
