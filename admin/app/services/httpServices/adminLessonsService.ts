// Generated by scaffold-service-from-controller — DO NOT EDIT BY HAND. Re-run when backend controllers change.
import { createAsyncThunk } from '@reduxjs/toolkit';
import { get } from '~/services/httpMethods/get';
import { post } from '~/services/httpMethods/post';
import { put } from '~/services/httpMethods/put';
import { patch } from '~/services/httpMethods/patch';
import { del } from '~/services/httpMethods/del';
import type * as LessonTypes from '~/types/lesson';

function __cleanQuery(q?: Record<string, unknown>): Record<string, unknown> | undefined {
  if (!q || typeof q !== 'object') return undefined;
  const out: Record<string, unknown> = {};
  for (const [k, v] of Object.entries(q)) {
    const t = typeof v;
    if (v != null && (t === 'string' || t === 'number' || t === 'boolean')) out[k] = v;
  }
  return Object.keys(out).length ? out : undefined;
}

export const list = createAsyncThunk(
  'lesson/list',
  async (query?: Record<string, unknown>) => {
    const res = await get('/admin/lessons', { params: __cleanQuery(query) });
    return res;
  },
);

export const detail = createAsyncThunk(
  'lesson/detail',
  async (id: string, query?: Record<string, unknown>) => {
    const res = await get(`/admin/lessons/${id}`, { params: __cleanQuery(query) });
    return res;
  },
);

// v103: aliases for naming conventions LLM-emitted pages tend to use.
// No runtime cost — same function reference re-exported under a verb-prefixed name.
export const fetchDetail = detail;

// v152: entity-canonical CRUD surface — the contract scaffold-crud-pages
// imports for every entity. Synthesized (not aliased) so ids and generated
// request INTERFACES stay assignable. See emitCanonicalCrudSurface().
export const fetchAdminLessonsList = createAsyncThunk(
  'adminLessons/fetchAdminLessonsList',
  async (params?: Record<string, unknown>) => {
    const res = await get<unknown>('/admin/lessons', { params: __cleanQuery(params) });
    return res;
  },
);

export const fetchAdminLessonsById = createAsyncThunk(
  'adminLessons/fetchAdminLessonsById',
  async (id: string) => {
    const res = await get<unknown>(`/admin/lessons/${id}`);
    return res;
  },
);

export async function createAdminLessons(data: unknown): Promise<unknown> {
  const res = await post<unknown>('/admin/lessons', data as Record<string, unknown>);
  return res;
}

export async function updateAdminLessons(id: string, data: unknown): Promise<unknown> {
  const res = await patch<unknown>(`/admin/lessons/${id}`, data as Record<string, unknown>);
  return res;
}

export async function deleteAdminLessons(id: string): Promise<void> {
  await del<void>(`/admin/lessons/${id}`);
}
