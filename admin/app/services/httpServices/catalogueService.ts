// Generated by scaffold-service-from-controller — DO NOT EDIT BY HAND. Re-run when backend controllers change.
import { createAsyncThunk } from '@reduxjs/toolkit';
import { get } from '~/services/httpMethods/get';
import { post } from '~/services/httpMethods/post';
import { put } from '~/services/httpMethods/put';
import { patch } from '~/services/httpMethods/patch';
import { del } from '~/services/httpMethods/del';
import type * as CatalogueTypes from '~/types/catalogue';

function __cleanQuery(q?: Record<string, unknown>): Record<string, unknown> | undefined {
  if (!q || typeof q !== 'object') return undefined;
  const out: Record<string, unknown> = {};
  for (const [k, v] of Object.entries(q)) {
    const t = typeof v;
    if (v != null && (t === 'string' || t === 'number' || t === 'boolean')) out[k] = v;
  }
  return Object.keys(out).length ? out : undefined;
}

export const listCategories = createAsyncThunk(
  'catalogue/listCategories',
  async (query?: Record<string, unknown>) => {
    const res = await get('/categories', { params: __cleanQuery(query) });
    return res;
  },
);

export const listCourses = createAsyncThunk(
  'catalogue/listCourses',
  async (query?: Record<string, unknown>) => {
    const res = await get('/courses', { params: __cleanQuery(query) });
    return res;
  },
);

export const getCourseBySlug = createAsyncThunk(
  'catalogue/getCourseBySlug',
  async (slug: string, query?: Record<string, unknown>) => {
    const res = await get(`/courses/${slug}`, { params: __cleanQuery(query) });
    return res;
  },
);

export const listInstructors = createAsyncThunk(
  'catalogue/listInstructors',
  async (query?: Record<string, unknown>) => {
    const res = await get('/instructors', { params: __cleanQuery(query) });
    return res;
  },
);

export const getInstructor = createAsyncThunk(
  'catalogue/getInstructor',
  async (id: string, query?: Record<string, unknown>) => {
    const res = await get(`/instructors/${id}`, { params: __cleanQuery(query) });
    return res;
  },
);

// v103: aliases for naming conventions LLM-emitted pages tend to use.
// No runtime cost — same function reference re-exported under a verb-prefixed name.
export const fetchGetCourseBySlug = getCourseBySlug;
export const fetchGetInstructor = getInstructor;

// v152: entity-canonical CRUD surface — the contract scaffold-crud-pages
// imports for every entity. Synthesized (not aliased) so ids and generated
// request INTERFACES stay assignable. See emitCanonicalCrudSurface().
export const fetchCatalogueList = createAsyncThunk(
  'catalogue/fetchCatalogueList',
  async (params?: Record<string, unknown>) => {
    const res = await get<unknown>('/categories', { params: __cleanQuery(params) });
    return res;
  },
);

export const fetchCatalogueById = createAsyncThunk(
  'catalogue/fetchCatalogueById',
  async (id: string) => {
    const res = await get<unknown>(`/courses/${id}`);
    return res;
  },
);

export async function createCatalogue(data: unknown): Promise<unknown> {
  const res = await post<unknown>('/', data as Record<string, unknown>);
  return res;
}

export async function updateCatalogue(id: string, data: unknown): Promise<unknown> {
  const res = await patch<unknown>(`//${id}`, data as Record<string, unknown>);
  return res;
}

export async function deleteCatalogue(id: string): Promise<void> {
  await del<void>(`//${id}`);
}
