/** Generated by scaffold-component-tests for app/pages/admin/adm-04-categories.tsx */
import { describe, expect, it } from 'vitest';
import { render, screen } from '@testing-library/react';
import { MemoryRouter } from 'react-router';
import { Provider } from 'react-redux';
import { configureStore } from '@reduxjs/toolkit';
import { I18nextProvider } from 'react-i18next';
import i18n from '~/i18n';
import Page from '~/pages/admin/adm-04-categories';

function wrap(ui: React.ReactNode) {
  const store = configureStore({ reducer: () => ({ auth: { user: null } }) });
  return (
    <Provider store={store}>
      <I18nextProvider i18n={i18n}>
        <MemoryRouter>{ui}</MemoryRouter>
      </I18nextProvider>
    </Provider>
  );
}

describe('adm-04-categories', () => {
  it('renders without crashing', () => {
    expect(() => render(wrap(<Page />))).not.toThrow();
  });

  it('renders root data-testid (adm-04-categories-page)', () => {
    render(wrap(<Page />));
    const root = screen.queryByTestId('adm-04-categories-page') ||
                 screen.queryByTestId('adm-04-categories-loading') ||
                 screen.queryByTestId('adm-04-categories-error');
    expect(root).toBeTruthy();
  });
});
