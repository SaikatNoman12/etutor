/**
 * Generated by scaffold-frontend-test-config. Vitest config for
 * component tests in tests/components/. Story specs (Playwright) live
 * separately under tests/stories/.
 */
import { defineConfig } from 'vitest/config';
import path from 'node:path';
import react from '@vitejs/plugin-react';
import tsconfigPaths from 'vite-tsconfig-paths';

export default defineConfig({
  plugins: [react(), tsconfigPaths()],
  // tsconfigPaths maps `~/x`, but a bare directory import (`~/i18n` → app/i18n/index.ts)
  // only resolves when the alias is a real filesystem root, so declare it explicitly.
  resolve: { alias: { '~': path.resolve(__dirname, '..', 'app') } },
  test: {
    environment: 'jsdom',
    globals: true,
    setupFiles: ['./tests/setup.ts'],
    include: ['tests/components/**/*.test.{ts,tsx}', 'tests/stores/**/*.test.{ts,tsx}'],
    coverage: {
      provider: 'v8',
      reporter: ['text', 'json-summary'],
      exclude: ['tests/**', 'node_modules/**', 'app/i18n/locales/**'],
    },
  },
});
