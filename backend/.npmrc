# This project's dependency graph needs the pre-npm-7 peer resolution: the generated
# package.json carries version skew that npm 7+ rejects outright. Declaring it here means
# `npm ci` on a build server installs exactly the way a developer does — without it the
# build fails on peer conflicts that never appear locally.
legacy-peer-deps=true
