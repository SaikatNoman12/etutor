// Generated by scaffold-auth-module
import { Module } from '@nestjs/common';
import { TypeOrmModule } from '@nestjs/typeorm';
import { PassportModule } from '@nestjs/passport';
import { User } from '../user/user.entity';
import { TokenModule } from '../../infrastructure/token/token.module';

import { AuthController } from './auth.controller';
import { AuthService } from './auth.service';
import { JwtStrategy } from '../../core/guards/jwt.strategy';

/**
 * AuthModule — generated by scaffold-auth-module. Provides the canonical
 * /auth/signup, /auth/login, /auth/refresh, /auth/logout, /auth/me surface.
 *
 * Depends on:
 *   - TokenModule (signs / verifies JWTs; exported globally so no provider here)
 *   - The User entity (passed via TypeOrmModule.forFeature)
 *   - PassportModule + JwtStrategy — registers the 'jwt' Passport strategy.
 *     The strategy class (core/guards/jwt.strategy.ts) is templated but
 *     self-registers ONLY when provided in a loaded module; without this every
 *     `AuthGuard('jwt')`/JwtAuthGuard route throws "Unknown authentication
 *     strategy jwt" → HTTP 500 (login still works because it is @Public(), so
 *     the gap is invisible until a guarded route is hit). See
 *     scaffold-jwt-strategy-registration-doctor (the post-LLM safety net).
 */
@Module({
  imports: [
    TypeOrmModule.forFeature([User]),
    TokenModule,
    PassportModule.register({ defaultStrategy: 'jwt' }),
  ],
  controllers: [AuthController],
  providers: [AuthService, JwtStrategy],
  exports: [AuthService, PassportModule, JwtStrategy],
})
export class AuthModule {}
