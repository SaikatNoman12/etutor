/**
 * Generated by scaffold-entities from PROJECT_DATABASE.md table `coupons`.
 * Edits to this file are preserved across re-runs ONLY when the table
 * definition is unchanged; otherwise re-run with --force to regenerate.
 */
import { Column, Entity } from 'typeorm';
import { BaseEntity } from '../../core/base/base.entity';
import { discount_type } from '../../common/enums/discount-type.enum';

@Entity('coupons')
export class Coupon extends BaseEntity {
  @Column({ type: 'varchar', length: 32, unique: true })
  code!: string;

  @Column({ name: 'discount_type', type: 'int', default: 1 })
  discountType!: discount_type;

  @Column({ name: 'discount_value', type: 'decimal', precision: 10, scale: 2, transformer: { to: (v: number | null) => v, from: (v: string | null) => (v === null ? null : Number(v)) } })
  discountValue!: number;

  @Column({ name: 'max_uses', type: 'int', default: 0 })
  maxUses!: number;

  @Column({ name: 'used_count', type: 'int', default: 0 })
  usedCount!: number;

  @Column({ name: 'valid_from', type: 'timestamptz', nullable: true })
  validFrom?: Date;

  @Column({ name: 'valid_until', type: 'timestamptz', nullable: true })
  validUntil?: Date;

  @Column({ name: 'is_active', type: 'boolean', default: true })
  isActive!: boolean;

}
