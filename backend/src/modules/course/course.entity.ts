/**
 * Generated by scaffold-entities from PROJECT_DATABASE.md table `courses`.
 * Edits to this file are preserved across re-runs ONLY when the table
 * definition is unchanged; otherwise re-run with --force to regenerate.
 */
import { Column, Entity, Index, JoinColumn, ManyToOne } from 'typeorm';
import { BaseEntity } from '../../core/base/base.entity';
import { Category } from '../category/category.entity';
import { User } from '../user/user.entity';
import { course_level } from '../../common/enums/course-level.enum';
import { course_status } from '../../common/enums/course-status.enum';

@Index('IDX_courses_status_published_at', ['status', 'publishedAt'])
@Index('IDX_courses_category_id', ['categoryId'])
@Index('IDX_courses_instructor_id', ['instructorId'])
@Entity('courses')
export class Course extends BaseEntity {
  @Column({ type: 'varchar', length: 200 })
  title!: string;

  @Column({ type: 'varchar', length: 200, unique: true })
  slug!: string;

  @Column({ type: 'varchar', length: 300, nullable: true })
  summary?: string;

  @Column({ type: 'text', nullable: true })
  description?: string;

  @Column({ name: 'thumbnail_url', type: 'varchar', length: 500, nullable: true })
  thumbnailUrl?: string;

  @Column({ type: 'decimal', precision: 10, scale: 2, transformer: { to: (v: number | null) => v, from: (v: string | null) => (v === null ? null : Number(v)) }, default: 0 })
  price!: number;

  @Column({ name: 'compare_at_price', type: 'decimal', precision: 10, scale: 2, transformer: { to: (v: number | null) => v, from: (v: string | null) => (v === null ? null : Number(v)) }, nullable: true })
  compareAtPrice?: number;

  @Column({ type: 'int', default: 1 })
  level!: course_level;

  @Column({ type: 'varchar', length: 20 })
  language!: string;

  @Column({ name: 'duration_minutes', type: 'int', default: 0 })
  durationMinutes!: number;

  @Column({ type: 'int', default: 0 })
  status!: course_status;

  @Column({ name: 'category_id', type: 'uuid' })
  categoryId!: string;

  @Column({ name: 'instructor_id', type: 'uuid' })
  instructorId!: string;

  @Column({ name: 'rating_avg', type: 'decimal', precision: 3, scale: 2, transformer: { to: (v: number | null) => v, from: (v: string | null) => (v === null ? null : Number(v)) }, default: 0 })
  ratingAvg!: number;

  @Column({ name: 'rating_count', type: 'int', default: 0 })
  ratingCount!: number;

  @Column({ name: 'student_count', type: 'int', default: 0 })
  studentCount!: number;

  @Column({ name: 'published_at', type: 'timestamptz', nullable: true })
  publishedAt?: Date;

  // ── Relations ──

  @ManyToOne(() => Category, { onDelete: 'CASCADE' })
  @JoinColumn({ name: 'category_id' })
  category?: Category;

  @ManyToOne(() => User, { onDelete: 'CASCADE' })
  @JoinColumn({ name: 'instructor_id' })
  instructor?: User;

}
