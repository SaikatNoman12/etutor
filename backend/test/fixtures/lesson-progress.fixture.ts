/**
 * Generated by scaffold-fixtures-per-entity. Inserts a LessonProgress row
 * directly via the repository. Useful for tests that need a pre-seeded record
 * without going through the full HTTP create flow.
 */
import { DataSource } from 'typeorm';

export interface CreateLessonProgressInput {
  [key: string]: unknown;
}

export async function createTestLessonProgress(
  dataSource: DataSource,
  overrides: CreateLessonProgressInput = {},
): Promise<Record<string, unknown>> {
  const meta = dataSource.entityMetadatas.find(
    (m) => m.name === 'LessonProgress' || m.tableName === 'lessonProgress',
  );
  if (!meta) {
    throw new Error('createTestLessonProgress: LessonProgress entity not registered with the DataSource');
  }
  const repo = dataSource.getRepository(meta.target);
  const row: Record<string, unknown> = {
    isCompleted: false,
    watchedSeconds: 0,
    completedAt: new Date(),
    ...overrides,
  };
  const saved = await repo.save(row as never);
  return saved as unknown as Record<string, unknown>;
}
