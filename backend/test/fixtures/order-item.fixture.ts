/**
 * Generated by scaffold-fixtures-per-entity. Inserts a OrderItem row
 * directly via the repository. Useful for tests that need a pre-seeded record
 * without going through the full HTTP create flow.
 */
import { DataSource } from 'typeorm';

export interface CreateOrderItemInput {
  [key: string]: unknown;
}

export async function createTestOrderItem(
  dataSource: DataSource,
  overrides: CreateOrderItemInput = {},
): Promise<Record<string, unknown>> {
  const meta = dataSource.entityMetadatas.find(
    (m) => m.name === 'OrderItem' || m.tableName === 'orderItems',
  );
  if (!meta) {
    throw new Error('createTestOrderItem: OrderItem entity not registered with the DataSource');
  }
  const repo = dataSource.getRepository(meta.target);
  const row: Record<string, unknown> = {
    titleSnapshot: 'test-titleSnapshot',
    unitPrice: 0,
    quantity: 0,
    ...overrides,
  };
  const saved = await repo.save(row as never);
  return saved as unknown as Record<string, unknown>;
}
