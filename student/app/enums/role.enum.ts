// Generated by scaffold-enum-sync — DO NOT EDIT BY HAND. Re-run if backend enum changes.

// Generated by scaffold-role-enum-from-spec — DO NOT EDIT BY HAND. Source: PROJECT_AUTH.yaml roles.
/**
 * role.enum.ts — this project's RBAC roles, as PROJECT_AUTH declares them.
 *
 * Values are the role NAMES; ROLE_VALUES below carries the numeric code each role is
 * stored as. RolesGuard canonicalises between the two, so either form may be passed to
 * @Roles().
 */
export enum RoleEnum {
  /** Browses the catalogue, buys courses, and works through the lessons they are enrolled in. UserRole value 1; def */
  student = 'student',
  /** Everything a student can do, plus owns courses and edits their own lessons. UserRole value 2; created by a sig */
  instructor = 'instructor',
  /** Runs the console over every course, user, order, enrollment and coupon. UserRole value 99; only an admin may c */
  admin = 'admin',
}

export type Role = `${RoleEnum}`;

export const ALL_ROLES: readonly RoleEnum[] = Object.values(RoleEnum);

/** The number each role is stored as in the database (PROJECT_AUTH `value`). */
export const ROLE_VALUES: Readonly<Record<Role, number>> = {
  student: 1,
  instructor: 2,
  admin: 99,
} as const;
