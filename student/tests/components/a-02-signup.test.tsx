/** Generated by scaffold-component-tests for app/pages/a-02-signup.tsx */
import { describe, expect, it } from 'vitest';
import { render, screen } from '@testing-library/react';
import { MemoryRouter } from 'react-router';
import { Provider } from 'react-redux';
import { configureStore } from '@reduxjs/toolkit';
import { I18nextProvider } from 'react-i18next';
import i18n from '~/i18n';
import Page from '~/pages/a-02-signup';

function wrap(ui: React.ReactNode) {
  const store = configureStore({ reducer: () => ({ auth: { user: null } }) });
  return (
    <Provider store={store}>
      <I18nextProvider i18n={i18n}>
        <MemoryRouter>{ui}</MemoryRouter>
      </I18nextProvider>
    </Provider>
  );
}

describe('a-02-signup', () => {
  it('renders without crashing', () => {
    expect(() => render(wrap(<Page />))).not.toThrow();
  });

  it('renders root data-testid (a-02-signup-page)', () => {
    render(wrap(<Page />));
    const root = screen.queryByTestId('a-02-signup-page') ||
                 screen.queryByTestId('a-02-signup-loading') ||
                 screen.queryByTestId('a-02-signup-error');
    expect(root).toBeTruthy();
  });
});
